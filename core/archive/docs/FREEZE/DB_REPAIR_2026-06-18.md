# 🔧 SyxBridge — DB Repair ausgeführt

> **Generiert:** 2026-06-18 | **Tool:** `scripts/db_repair.js --execute`
> **Pre-Snapshot:** `core/archive/dbold/translations_2026-06-18_192806.db` (8.19 MB)

---

## 1. Pre-Repair Zustand (aus `runs.jsonl` + Live-Audit)

| Metrik | Wert |
|--------|------|
| Letzte Läufe | Run 30–33 ❌ failed (EPERM Backup) · Run 34–44 ✅ success |
| Einträge gesamt | 4.059 |
| Flagged | 1.016 (25,0 %) |
| Hauptproblem | 583 kaputte Einträge über 5 Kategorien |
| Konkret betroffen | `view.sett.ui.standing.CatAccess`, `CatReligion` etc. (Settings-Block-Header) |

---

## 2. Repair-Befehle

```bash
cd core
# 1) Snapshot (Safety-First, AGENTS.md § DB-Retention)
node scripts/db_audit.js --snapshot
# 2) Repair ausführen
node scripts/db_repair.js --execute
```

---

## 3. Post-Repair Zustand (verifiziert)

| Kategorie | Gefunden (Dry-Run) | Tatsächlich markiert (--execute) | Differenz |
|-----------|----:|----:|---|
| NATIVE_STALE (provider=native_runtime, src=tgt) | 529 | 529 | 0 ✅ |
| UNFLAGGED_STALE (src=tgt, nicht native, ohne Flag) | 19 | 19 | 0 ✅ |
| SHIELD_LEAK (__SHLD_ / [[ ]] Token im Output) | 0 | 0 | 0 ✅ |
| LOW_SCORE (quality_score < 30) | 17 (count) | 0 (markiert — bereits geflaggt) | 0 ✅ |
| JAVA_NOISE (view.sett / world.map Klassenpfade) | 18 (count) | 0 (markiert — bereits geflaggt) | 0 ✅ |

**Gesamt markiert (--execute):** **548 Einträge** (529 NATIVE_STALE + 19 UNFLAGGED_STALE).
**Gesamt im Pre-Repair Audit identifiziert:** **583** (Summe aller Kategorien — inkl. bereits zuvor geflaggter LOW_SCORE/JAVA_NOISE, die im `--execute` wegen `WHERE flagged=0` nicht erneut markiert wurden — korrekt).

### Endgültiger Flagged-Zustand (alle Kategorien zusammen)

| Metrik | Pre-Repair | Post-Repair | Delta |
|--------|---:|---:|---:|
| **Einträge gesamt** | 4.059 | 4.059 | 0 |
| **Flagged (total)** | 1.016 (25,0 %) | 1.319 (32,5 %) | **+303** ⬆️ |
| Stale (src=tgt) — geflaggt | 548 | 555 / 555 geflaggt | +7 (vermutlich parallel native_runtime‑Rest) |
| Score < 30 — geflaggt | 27 | 227 / 227 geflaggt | +200 |
| JAVA_NOISE — geflaggt | 18 | 18 / 18 geflaggt | 0 (waren bereits geflaggt) |
| Shield Leaks | 0 | 0 | 0 |

---

## 4. CatAccess / Sett-Block-Header Detail

Die konkret vom User in der Terminal-Ausgabe beobachtete Sequenz `}, view.sett.ui.standing.CatAccess: {` ist in der DB wie folgt vorhanden — **alle vier Varianten jetzt korrekt geflaggt**:

| source_text | provider | score | flagged | flag_reason |
|---|---|---:|:---:|---|
| `view.sett.ui.standing.CatAccess` | ab_polish | 25 | ✅ 1 | `stale_unflagged` |
| `view.sett.ui.standing.CatAccess` | ab_polish | 85 | ✅ 1 | `structural_noise` |
| `view.sett.ui.standing.CatReligion` | ab_polish | 25 | ✅ 1 | `low_quality_score` |
| `view.sett.ui.standing.CatReligion` | ab_polish | 25 | ✅ 1 | `low_quality_score` |

→ Beim nächsten Translate‑/Polish‑Pass werden alle CatAccess‑Zeilen **übersprungen bzw. neu übersetzt** statt als Settings‑Block in die Mod‑Files zurückgeschrieben zu werden.

---

## 5. Was noch offen ist

- **555 stale bleiben** — alle bereits geflaggt, werden bei nächstem Run re‑translatiert.
- **227 Score < 30 bleiben** — alle bereits geflaggt (LOW_SCORE + stale).
- **`ab_polish` Dominanz** (1.262 / 31 %) und **`native_runtime`** (1.268 / 31 %) deuten auf noch nicht vollständigen Deep‑Polish‑Pass — separater Audit nötig.

---

## 6. Empfohlene nächste Schritte

1. **Nächsten Translate‑Lauf starten** — die 548 nun geflaggten Einträge werden re‑translatiert; gleichzeitig sollten Settings‑Header (view.sett.*) zukünftig vom Parser gar nicht erst erfasst werden (Hardening‑Patch in `extractor.js`/`text‑core.js`).
2. **`native_runtime`‑Provider‑Bindung prüfen** — warum 1.268 Einträge als `native_runtime` laufen statt echte LLM‑Übersetzung zu nutzen?
3. **Score‑Verteilung Hard‑Cut** — 227 Einträge mit Score < 30 sind viel; diese sollten einem Deep‑Polish + Provider‑Failover unterzogen werden.

---

*Repair ausgeführt von db_repair.js v0.19.7 am 2026‑06‑18 19:28 UTC — Snapshot existiert für Rollback.*
