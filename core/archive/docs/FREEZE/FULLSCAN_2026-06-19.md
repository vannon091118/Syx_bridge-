# 🔍 SyxBridge v0.19.9 — Fullscan-Report

> **Generiert:** 2026-06-19 | **Branch:** prepare-0.20-wip | **Version:** v0.19.9
> **Auftrag:** Vollständiger Code-Scan, Dependency-Matrix, Hardcode-Analyse, Dead Code, Call-Chains, Prioritäten

---

## ════════════════════════════════════════════════════════
## TEIL 1 — FUNKTIONS-INVENTAR (215 Funktionen, 32 Dateien)
## ════════════════════════════════════════════════════════

### 1.1 Kernmodule (Core Translation Pipeline)

| Datei | Funktionen | Zeilen | Rolle |
|-------|-----------|--------|-------|
| `translation-runtime.js` | 11 | ~853 | Orchestrator: translateBatch, ensureTranslations, fixGrammarBatch, runDeepPolishBatch, googleFreePreflight, translateBatchWithRouting, flagPotentialErrors, getBestAvailableQualityModel, parseBatchResponseWithMaps, buildBatchPromptForCurrentConfig |
| `translation-quality.js` | 7 | ~155 | Quality: isLikelyTargetLanguageText, classifyNativeDecision, scoreTranslationQuality, inferFlagReason, checkTerminologyViolations, getGuardedTerminology |
| `translation-db.js` | 10 | ~295 | DB/Caching: getEntryHash, normalizeInputs, buildGlossaryMap, loadGlossaryRows, enrichWithContext, learnGlossary, saveStressTestResult, getCachedTranslations, saveTranslation |
| `text-core.js` | 17 | ~440 | Text Processing: normalizePromptItem, protectPlaceholders, isProperNoun, classifyPath, shouldTranslate, stripJsonFence, extractJsonPayload, summarizeGrammarContext, cleanTranslationArtifact, parseBatchResponse, buildBatchPrompt, buildProofreadPrompt, placeholdersValid, translationCriticalCheck, assessTranslationWarnings, translationLooksSafe, restoreAndValidateTranslation, extractReplacements, applyTranslations |
| `context-packets.js` | 6 | ~190 | Risk Scoring: normalizeTranslationEntry, mergeEntryContexts, countMatches, scoreTranslationRisk, scoreDynamicRisk, buildContextPacket |
| `dispatcher.js` | 5 | ~160 | Routing: resolveProviderModel, isQualityProvider, buildStageRoutePlan, resolveTranslateRoute, runRoute |

### 1.2 Provider & Config

| Datei | Funktionen | Zeilen | Rolle |
|-------|-----------|--------|-------|
| `providers/client-factory.js` | 14 | ~580 | LLM Clients: normalizeWhitespace, buildGeminiSchema, buildGeminiRequest, getBatchProfile, handleRateLimits, callGeminiBatch, callGroqBatch, callOpenRouterBatch, callArgosBatch, callGoogleTranslateFree, callOllamaBatch, callPlayer2Batch, callNvidiaBatch, callFcmBatch, executeStageRequest |
| `config-runtime.js` | 28 | ~880 | Config+Discovery: firstDefined, parseDryRunFlag, isDryRun, resetDryRunCache, getGateCounterOpts, parseEnvFlag, parseKeys, isUsableTextModel, rankModel, filterLLMs, getDefaultModelForProvider, maskSecret, Class(ConfigRuntime): getApiKey, getProviderStatus, updateProviderRateLimit, markKeyStatus, resetValidationStats, markKeyCooldown, rotateApiKey, markProviderDegraded, isProviderHealthy, testApiKey, fetchGeminiModels, fetchGroqModels, fetchOllamaModels, fetchPlayer2Models, fetchNvidiaModels, fetchFcmModelRankings, enhanceModelListWithFcm, checkCloudKey, checkLocalProvider, withRetry, ensureGroqModel, ensureOllamaModel, ensurePrimaryModel, configure, checkConfig, showApiStatus, persistConfigToEnv, persistSingleEnvVar |
| `router.js` | 4 | ~270 | Provider Routing: isFreeModel, estimateCostClass, isEnabledFlag, Class(Router) |
| `model-registry.js` | 6 | ~220 | Model Discovery: createModelRegistry, getModelStatus, installTargetLanguage, startOllamaPull, getActivePulls, isOllamaModelInstalled |

### 1.3 Write Pipeline

| Datei | Funktionen | Zeilen | Rolle |
|-------|-----------|--------|-------|
| `exporter.js` | 4 | ~105 | File Output: ensureDir, mergeRecursive, writeTranslatedFile, bundleBridgeCore |
| `extractor.js` | 7 | ~121 | String Extraction: getHash, unescapeTextValue, escapeTextValue, classifyString, extractStrings, shieldPlaceholders, restorePlaceholders |
| `parser.js` | 5 | ~190 | Format Parsing: registerFormat, parseSos, parseRaw, parseJson, detectFormat, parse, listFormats |
| `validator.js` | 7 | ~160 | Validation: validatePlaceholders, validateTags, checkStructure, classifyStructureIssues, getQaScore, validateFileSyntax |

### 1.4 Game-Specific Adapters & Plugins

| Datei | Funktionen | Zeilen | Rolle |
|-------|-----------|--------|-------|
| `adapters/GameAdapter.js` | 14 (abstract) | ~100 | Interface: getMetadataFileName, parseMetadata, formatMetadata, getCoreModFolderName, getCoreModMetadata, applyPatchModifications, getBackupDirectoryName, isBackupDirectory, isVersionDirectory, getOverrideHeader, formatPatchNotice, getParserFormat, classifyFile, isTranslatableFile, scanMod |
| `adapters/SongsOfSyxAdapter.js` | 14 (concrete) | ~180 | SoS Implementation: alle GameAdapter-Methoden |
| `plugins/GamePlugin.js` | 4 (abstract) | ~55 | Plugin Interface: serializeTranslation, validateTranslation, getPromptContext, getFileHeader |
| `plugins/SongsOfSyxPlugin.js` | 18 (concrete) | ~200 | SoS Plugin: alle GameAdapter + GamePlugin Methoden |

### 1.5 Orchestrierung & Infrastruktur

| Datei | Funktionen | Zeilen | Rolle |
|-------|-----------|--------|-------|
| `index.js` | ~15 | ~720 | Entry Point: initDb, applyEnvToConfig, main, synchronize, writeTranslatedFile, readFileJobWithAdapter, readCurrentModConfig, collectTextFiles |
| `planner.js` | 3 | ~240 | Mod Processing: processFile, processMod, run |
| `scanner.js` | 3 | ~60 | File Scanning: classifyFile, scanMod, collectFiles |
| `runtime-ops.js` | 5 | ~240 | Runtime Ops: createRuntimeOps, ensureCoreMod, mergePatchesToCore, translateMod, setupGateCounter, flushGateCounter |
| `gui-handlers.js` | 5 | ~610 | GUI API: readDisplayName, restoreBackup, collectAllFiles, startStatsBroadcast, registerGuiHandlers |
| `gui/server.js` | Class | ~450 | HTTP/SSE Server: GuiServer |
| `sos-runtime.js` | 4 | ~80 | SoS Launcher: parseSoSConfig, stringifySoSConfig, getActiveMods, syncLauncherSettings |
| `db.js` | 8 | ~300 | Database: connect, connectReadOnly, allReadOnly, run, get, all, init, migrateRiskScore |
| `diagnostics.js` | 2 | ~50 | Diagnostics: runDiagnostics, clearCache |
| `logger.js` | 5 | ~110 | Logging: formatLogValue, writeLog, setDb, setupLogging, logRun, logPayload |
| `gate-counter.js` | 6 | ~90 | Gate Tracking: resolveRunsPath, createGateCounter, record, summarize, flush, getGateCounter |
| `cli-progress.js` | 14 | ~260 | CLI Progress: pad, elapsedStr, etaStr, throughputStr, bar, pctStr, buildLines, clearPrevious, render, startPhase, updateMod, updateBatch, tick, addOk, addErr, addCache, finish, isActive |
| `polish-arbiter.js` | 5 | ~224 | A/B Polish: scorePolishVariant, selectDiverseProviders, executePolishWithProvider, pickBestPerEntry, runAbPolishing |
| `glossary.js` | 3 | ~45 | Glossary: shouldLearnGlossaryTerm, normalizeGlossaryRows, findRelevantGlossaryTerms |
| `ui.js` | 3 | ~70 | CLI Menu: mainMenu, selectMod, confirm |

---

## ════════════════════════════════════════════════════════
## TEIL 2 — DEPENDENCY-MATRIX (Import-Graph)
## ════════════════════════════════════════════════════════

### 2.1 Kritische Pfade (wer importiert wen)

```
index.js (ENTRY POINT)
  ├→ adapters/SongsOfSyxAdapter
  ├→ plugins/SongsOfSyxPlugin
  ├→ config-runtime.js (ConfigRuntime, persistConfigToEnv)
  ├→ router.js (Router)
  ├→ db.js (init, get, all, run)
  ├→ planner.js (Planner)
  ├→ runtime-ops.js (createRuntimeOps)
  ├→ translation-runtime.js (createTranslationRuntime)
  ├→ gui-handlers.js (registerGuiHandlers)
  ├→ model-registry.js (createModelRegistry)
  ├→ sos-runtime.js (getActiveMods, syncLauncherSettings)
  ├→ logger.js (setupLogging, logRun)
  ├→ ui.js (mainMenu, selectMod)
  ├→ exporter.js (writeTranslatedFile, bundleBridgeCore)
  ├→ parser.js (parse, detectFormat)
  ├→ diagnostics.js (runDiagnostics)
  └→ text-core.js (shouldTranslate, protectPlaceholders, etc.)

translation-runtime.js (ORCHESTRATOR)
  ├→ context-packets.js
  ├→ dispatcher.js
  ├→ polish-arbiter.js
  ├→ cli-progress.js
  ├→ providers/client-factory.js
  ├→ translation-quality.js
  └→ translation-db.js

translation-db.js
  ├→ context-packets.js
  └→ glossary.js

text-core.js
  ├→ extractor.js
  └→ validator.js

exporter.js
  ├→ text-core.js
  ├→ validator.js
  └→ gate-counter.js

dispatcher.js
  ├→ gate-counter.js
  └→ text-core.js (classifyPath)

parser.js
  └→ extractor.js

planner.js
  ├→ db.js
  ├→ scanner.js
  ├→ parser.js
  ├→ extractor.js
  ├→ logger.js
  └→ cli-progress.js

scanner.js
  └→ adapters/SongsOfSyxAdapter

runtime-ops.js
  ├→ gate-counter.js
  └→ config-runtime.js
```

### 2.2 Zirkuläre Imports

**Keine.** Der Dependency-Graph ist ein DAG (Directed Acyclic Graph).

### 2.3 Blätter (keine internen Dependencies)

| Datei | Exportiert |
|-------|-----------|
| `context-packets.js` | normalizeTranslationEntry, mergeEntryContexts, scoreTranslationRisk, scoreDynamicRisk, buildContextPacket |
| `glossary.js` | shouldLearnGlossaryTerm, normalizeGlossaryRows, findRelevantGlossaryTerms |
| `cli-progress.js` | startPhase, updateMod, updateBatch, tick, addOk, addErr, addCache, finish, isActive |
| `extractor.js` | extractStrings, getHash, classifyString, unescapeTextValue, escapeTextValue, shieldPlaceholders, restorePlaceholders |
| `adapters/GameAdapter.js` | GameAdapter (abstract class) |

---

## ════════════════════════════════════════════════════════
## TEIL 3 — CALL CHAINS (Kritische Pfade)
## ════════════════════════════════════════════════════════

### 3.1 Haupt-Übersetzungspfad (CLI)

```
index.js:main()
  → synchronize(planner, options)
    → planner.run('sync', mods, options)
      → planner.processMod(mod)
        → planner.processFile(file)
          → parser.parse(content, {format: 'sos'})
            → extractor.extractStrings(content)
        → hooks.translateMod(mod)
          → runtime-ops.translateMod(modDir)
            → collectTextFiles(modDir, adapter)
            → readFileJob(filePath) → parser.parse()
            → ensureTranslations(allTexts, options)          [translation-runtime.js]
              → enrichWithContext(items)                      [translation-db.js]
              → getCachedTranslations(items)                  [translation-db.js]
              → classifyNativeDecision(entry, glossaryMap)    [translation-quality.js]
              → translateBatchWithRouting(currentBatch)
                → dispatcher.runRoute('translate', ...)
                  → translateBatch(items, route)
                    → protectPlaceholders(entry.source)       [text-core.js]
                    → clients.call[Provider]Batch(entries)    [client-factory.js]
                    → restoreAndValidateTranslation(...)      [text-core.js]
                    → translationCriticalCheck(...)           [text-core.js]
              → saveTranslation(entry, translated, ...)      [translation-db.js]
              → learnGlossary(source, translated, ...)        [translation-db.js]
              → flagPotentialErrors(batchValues)
              → polishArbiter.runAbPolishing(entries)         [polish-arbiter.js]
              → fixGrammarBatch(entries, 'polish')
              → runDeepPolishBatch(targetLang)
            → writeTranslatedFile(job, outputPath, translations)
              → exporter.writeTranslatedFile(...)
                → plugin.serializeTranslation(translated, r)  [SongsOfSyxPlugin.js]
                → plugin.getFileHeader(outputPath)             [SongsOfSyxPlugin.js]
                → validateFileSyntax(content, newContent)      [validator.js]
```

### 3.2 GUI-Pfad

```
index.js
  → registerGuiHandlers({...})                              [gui-handlers.js]
    → GuiServer (HTTP/SSE on :3000)                         [gui/server.js]
      → POST /api/action/sync
        → emit('action', 'sync')
          → synchronize(planner, options)                    [→ Call Chain 3.1]
      → POST /api/action/check-db
        → runDiagnostics()                                  [diagnostics.js]
        → ensureTranslations(lowQuality, {forcePolish})
      → GET /api/fcm-rankings
        → configRuntime.fetchFcmModelRankings()
      → POST /api/key-check
        → configRuntime.checkCloudKey(provider, key)
```

### 3.3 Plugin vs Adapter

| Aspekt | GameAdapter | GamePlugin |
|--------|-------------|------------|
| **Verwendung** | scanner.js, runtime-ops.js, planner.js | text-core.js, exporter.js, index.js |
| **Fokus** | Dateistruktur, Metadata, Backup | Content-Serialisierung, LLM-Prompts, Validierung |
| **Injection** | Direkt instanziiert in scanner.js | Side-Channel: `buildBatchPrompt._plugin = activePlugin` |
| **Geplant** | Durch Plugin ersetzen (v0.20) | Erweitern um fehlende Adapter-Methoden |

---

## ════════════════════════════════════════════════════════
## TEIL 4 — HARDCODED SONGS OF SYX ELEMENTE
## ════════════════════════════════════════════════════════

### 4.1 Klassifizierung

| Kategorie | Datei | Stelle | Was | Schwere |
|-----------|-------|--------|-----|---------|
| **LLM-Prompts** | `text-core.js` | L215 | `gameName: 'Songs of Syx'` Fallback in buildBatchPrompt | 🟡 Mittel |
| **LLM-Prompts** | `text-core.js` | L265 | `"You are a senior editor for 'Songs of Syx' game localizations."` | 🔴 Hoch |
| **LLM-Prompts** | `text-core.js` | L215 | `"medieval, professional, and slightly gritty tone"` | 🟡 Mittel |
| **Path-Rules** | `text-core.js` | L60 | `PATH_RULES`: `bio/specific`, `race/name`, `room/`, `tech/`, `subject/` | 🔴 Hoch |
| **Risk-Scoring** | `context-packets.js` | L85 | `(kingdom\|empire\|clan\|tribe\|guild\|order\|house\|dynasty)` | 🟡 Mittel |
| **Language Detection** | `translation-quality.js` | L45 | `(battle\|room\|workers\|efficiency\|hunting\|city\|food\|population)` | 🟡 Mittel |
| **File Format** | `extractor.js` | L54 | `KEY: "value"` Regex (SoS-Format) | ✅ Bereits im Plugin |
| **File Format** | `exporter.js` | L37 | `outputPath.includes('V71') && !newContent.includes('__OVERWRITE')` | ✅ Bereits im Plugin |
| **Mod Metadata** | `gui-handlers.js` | L11 | `readDisplayName()` erwartet `_Info.txt` mit `NAME:` | 🟡 Mittel |
| **Launcher** | `sos-runtime.js` | L13 | `songsofsyx/settings/LauncherSettings.txt` APPDATA-Pfade | ✅ SoS-only OK |
| **UI** | `ui.js` | L6 | `"SONGS OF SYX - AI BRIDGE BUILDER"` | 🟢 Niedrig |
| **Steam** | `index.js` | L~50 | `content\\1162750` Workshop App-ID | 🟢 Niedrig |
| **Scanner** | `scanner.js` | L3 | `SongsOfSyxAdapter` als Default-Adapter | ✅ Bereits im Adapter |
| **Adapter** | `scanner.js` | L14 | `classifyFile()` Fallback ohne Adapter | 🟡 Mittel |

### 4.2 Was bereits im Plugin/Adapter ist (v0.19.9)

Diese Elemente sind **bereits korrekt gekapselt:**

- ✅ `serializeTranslation()` → SongsOfSyxPlugin
- ✅ `getFileHeader()` (__OVERWRITE) → SongsOfSyxPlugin
- ✅ `getPromptContext()` → SongsOfSyxPlugin (wenn `buildBatchPrompt._plugin` gesetzt)
- ✅ `validateTranslation()` → SongsOfSyxPlugin
- ✅ `classifyFile()`, `scanMod()`, `parseMetadata()` → SongsOfSyxAdapter
- ✅ `getParserFormat()` → SongsOfSyxAdapter
- ✅ `extractStrings()` Regex → extractor.js (SoS-Format, wird vom Plugin genutzt)

### 4.3 Was NOCH im Core steckt (muss ins Plugin)

| # | Element | Datei | Aktion |
|---|---------|-------|--------|
| **H1** | `"You are a senior editor for 'Songs of Syx'..."` | `text-core.js:buildProofreadPrompt` | → `plugin.getPromptContext().gameName` |
| **H2** | `PATH_RULES: bio/specific, race/name, room/, tech/` | `text-core.js:PATH_RULES` | → `plugin.getPathRules()` oder Adapter |
| **H3** | `gameName: 'Songs of Syx'` Fallback | `text-core.js:buildBatchPrompt` | → bereits via `_plugin`, Fallback noch hart |
| **H4** | `(kingdom\|empire\|clan\|tribe\|guild)` | `context-packets.js:scoreTranslationRisk` | → `plugin.getLoreTerms()` |
| **H5** | `(battle\|room\|workers\|efficiency\|hunting)` | `translation-quality.js:isLikelyTargetLanguageText` | → `plugin.getGameTerms()` |
| **H6** | `readDisplayName()` → `_Info.txt` NAME: | `gui-handlers.js` | → `adapter.getMetadataFileName()` + `parseMetadata()` |
| **H7** | `classifyFile()` Fallback ohne Adapter | `scanner.js` | → `adapter.classifyFile()` (Default-Weg) |
| **H8** | `"SONGS OF SYX - AI BRIDGE BUILDER"` | `ui.js` | → `plugin.getGameName()` oder Config |

---

## ════════════════════════════════════════════════════════
## TEIL 5 — DEAD CODE KANDIDATEN
## ════════════════════════════════════════════════════════

| # | Datei | Funktion | Status | Empfehlung |
|---|-------|----------|--------|------------|
| **D1** | `validator.js` | `getQaScore()` | Exportiert, nicht importiert. Ersetzt durch `scoreTranslationQuality()` in `translation-quality.js`. | 🗑️ Entfernen |
| **D2** | `parser.js` | `listFormats()` | Exportiert, nicht aufgerufen. Debugging-Util. | 🗑️ Entfernen |
| **D3** | `diagnostics.js` | `clearCache()` | Exportiert, nur in `gui-handlers.js` indirekt referenziert aber nie aufgerufen. | ⚠️ Prüfen ob GUI-Endpoint existiert |
| **D4** | `exporter.js` | `mergeRecursive()` | Intern von `bundleBridgeCore()` genutzt. Nicht tot. | ✅ Behalten |
| **D5** | `index.js` | Root-Exports (`shouldTranslate`, `protectPlaceholders`, etc.) | Exportiert für Tests, aber `index.js` ist CLI-Entry. | ⚠️ In Test-Utils auslagern |

---

## ════════════════════════════════════════════════════════
## TEIL 6 — ONLINE-RECHERCHE: Songs of Syx Plugin-Verifizierung
## ════════════════════════════════════════════════════════

### 6.1 Verifizierte Fakten (4rg0n/songs-of-syx-mod-example + mod.io)

| Aspekt | Online-Doku | SyxBridge-Implementierung | Status |
|--------|-------------|--------------------------|--------|
| **Dateiformat** | `.txt` mit `KEY: "value"` | `extractor.js` Regex `/([a-zA-Z0-9_]+):\s*"((?:\\.|[^"\\])*)"/g` | ✅ Korrekt |
| **Metadaten** | `_Info.txt` mit VERSION, GAME_VERSION_MAJOR, NAME, DESC, AUTHOR | `SongsOfSyxAdapter.parseMetadata()` | ✅ Korrekt |
| **Encoding** | UTF-8 | Node.js default (UTF-8) | ✅ Korrekt |
| **V71 __OVERWRITE** | `__OVERWRITE: true` Header für Override-Mods | `SongsOfSyxPlugin.getFileHeader()` | ✅ Korrekt |
| **Mod-Pfade** | `assets/init/room/`, `assets/init/tech/`, `text/tech/` | `SongsOfSyxAdapter.classifyFile()` | ✅ Korrekt |
| **Launcher** | `%APPDATA%/songsofsyx/settings/LauncherSettings.txt` | `sos-runtime.js` | ✅ Korrekt |
| **Steam Workshop** | App-ID `1162750` | `index.js` Workshop-Pfad | ✅ Korrekt |
| **Übersetzung** | Dictionary-Dateien in `assets/text/` | SyxBridge schreibt in `text/[LANG]/` | ✅ Korrekt |
| **JSON-Unterstützung** | Nein, nur `.txt` | `parser.js` hat `json`-Format registriert | ⚠️ Überflüssig für SoS, aber für andere Spiele nützlich |

### 6.2 Abweichungen

| Punkt | Online | SyxBridge | Bewertung |
|-------|--------|-----------|-----------|
| **`¤¤`-Prefix** | SoS nutzt `¤¤save = "Saving"` für injizierte Strings | SyxBridge ignoriert diese (keine `¤¤` im Regex) | ✅ Korrekt — diese werden nicht übersetzt |
| **Code-Mods (.jar)** | SoS unterstützt Java-Code-Mods | SyxBridge ignoriert `.jar` | ✅ Korrekt — nur Text-Mods |
| **`D.ts(ClassName.class)`** | Engine-internal Translation-Injection | Nicht relevant für Bridge | ✅ OK |

---

## ════════════════════════════════════════════════════════
## TEIL 7 — PRIORISIERTER AKTIONSPLAN
## ════════════════════════════════════════════════════════

### 7.1 Cross-Reference mit bestehenden Reports

| Report | Kernbefund | Dieser Report bestätigt |
|--------|-----------|------------------------|
| `LOG_REPORT_2026-06-19.md` | Argos tot (F1), 99.7% Stage-0, google_free dominant | ✅ — `check_argos.js` Python SyntaxError, kein Circuit-Breaker |
| `AUDIT_REPORT_2026-06-17.md` | callGoogleTranslateFree Referenz-Fehler | ✅ — Bereits gefixt in v0.19.6 |
| `SESSION_REPORT_2026-06-18_BUGFIX-SPRINT.md` | NVIDIA Batch-Client fehlte, migrateRiskScore Crash | ✅ — Beides gefixt in v0.19.7 |
| `TRANSLATION_RUNTIME_SPLIT_2026-06-18.md` | Plugin-Architektur + Runtime-Split | ✅ — Implementiert in v0.19.9 (diese Session) |

### 7.2 Prioritäten (konsolidiert)

| Prio | Aufgabe | Aufwand | Effekt | Abhängigkeit |
|------|---------|---------|--------|-------------|
| 🔴 **P0** | **Circuit-Breaker** für Provider (nach 3×404 → deaktivieren) | ~1h | Verhindert verbrannte API-Keys | — |
| 🔴 **P0** | **Shield-Leak Fix** (diese Session implementiert) | ✅ Fertig | DB sauber | — |
| 🟠 **P1** | **H1: buildProofreadPrompt** → Plugin-delegieren | ~30m | Multi-Game-fähig | Plugin existiert |
| 🟠 **P1** | **H2: PATH_RULES** → Plugin/Adapter | ~1h | Multi-Game-fähig | Plugin erweitern |
| 🟠 **P1** | **H4+H5: Risk/Quality Heuristics** → Plugin | ~1h | Multi-Game-fähig | Plugin erweitern |
| 🟡 **P2** | **Argos Fix** (F1: spawnSync Escaping) | ~30m | Offline-Fallback | — |
| 🟡 **P2** | **Dead Code entfernen** (D1, D2) | ~15m | Sauberkeit | — |
| 🟡 **P2** | **H6: gui-handlers.js** → Adapter nutzen | ~30m | Konsistenz | — |
| 🟢 **P3** | **H8: ui.js** → Config/Plugin Game-Name | ~10m | Kosmetik | — |
| 🟢 **P3** | **H3: buildBatchPrompt Fallback** → entfernen | ~5m | Sauberkeit | H1 erledigt |
| 🟢 **P3** | **RimWorld-Prototyp** | ~4h | Machbarkeitsnachweis | H1-H5 erledigt |

### 7.3 Effort-to-Next-Scope

| Scope | Voraussetzung | Aufwand |
|-------|--------------|---------|
| **RimWorld-Support** | H1-H5 (Hardcode→Plugin) + `registerFormat('xml')` | ~6h |
| **Stellaris-Support** | RimWorld-Support + Clausewitz-Format-Parser | ~8h |
| **Generischer Game-Adapter** | H1-H8 + Auto-Detection + Plugin-Framework | ~12h |

---

## ════════════════════════════════════════════════════════
## TEIL 8 — METRIKEN
## ════════════════════════════════════════════════════════

| Metrik | Wert |
|--------|------|
| **Dateien gescannt** | 32 (.js in core/src/ + index.js) |
| **Funktionen inventarisiert** | 215 |
| **Module-Exporte** | 27 |
| **Import-Ketten** | 34 |
| **Zirkuläre Imports** | 0 |
| **Hardcoded SoS-Stellen** | 14 (davon 6 bereits im Plugin, 8 noch im Core) |
| **Dead-Code-Kandidaten** | 5 (2 sicher, 2 zu prüfen, 1 OK) |
| **Kritische Call-Chains** | 3 (CLI, GUI, Plugin) |
| **Online-Verifizierung** | 9/9 Punkte bestätigt |

---

*Report generiert von Buffy (Codebuff) — Fullscan + Dependency-Matrix + Hardcode-Analyse + Online-Recherche*
*Basis: v0.19.9 (Plugin-Architektur + Runtime-Split + Shield-Leak Fix)*
