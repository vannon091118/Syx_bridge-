P8-2: Foreign Key Cascades für alle 5 FKs implementiert. Approach B+C: ON DELETE CASCADE in CREATE TABLE (neue DBs) + 5 BEFORE DELETE Triggers als Polyfill (existierende DBs). Schema v7→v8. Zero-daten-migration, Tabellen-Neuerstellung nicht nötig. Triggers löschen Kindzeilen vor FK-Check.
