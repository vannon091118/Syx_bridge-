Extrahiere den CLI-Wizard configure() (110 LOC) aus ConfigRuntime-Klasse nach config-wizard.js.

configureWizard(cr, persistConfigToEnv) — standalone async function, alle this.-Referenzen auf cr. umgestellt.
ConfigRuntime.configure() bleibt als thin Delegation: async configure() { return configureWizard(this, persistConfigToEnv); }
checkConfig() ruft weiter this.configure() — keine Breakage.

Nebenbefund: toter prompts-Import in config-runtime.js entfernt (nur configure() hatte prompts benutzt).
