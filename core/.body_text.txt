author_system.js: buildSubject() — Jeder Erzaehler hat jetzt einen eigenen Titelstil. Basher mit Datei-Count, Ghost mit "verzeichnet:", Glitch mit "ermittelt:", Squizzle mit "s Fall:", Echo mit "erinnert:", Spark mit "entdeckt:", Sage mit "lehrt:", etc. Impulse-Text wird auf ~50 Zeichen gekuerzt. Keine 150-Zeichen-Titel mehr.

resolvePlaceholders() — FILE, COUNT, HASH, DATE, TIME und 50 weitere Platzhalter-Typen werden VOR der Filterung mit echten Werten befuellt: erster gestagter Dateiname, Commit-Hash, aktuelle Zeit, etc. Die 55 Template-Eintraege im Sidejoke-Pool sind jetzt nutzbar statt totes Gewicht. Syntax OK, alle Tests gruen.