checkAllKeys DOM-Polling eliminiert. checkSingleKey() gibt jetzt seine Promise-Kette zurück. checkAllKeys() chained direkt auf checkSingleKey().then(result => ...) statt setInterval-basiertes Button-Text-Polling alle 100ms. Defensive .catch() pro Key verhindert dass einzelne Fehler die gesamte Kette brechen. Backward-kompatibel: HTML onclick ignoriert Rückgabewert.

Code-Review bestätigt: Promise-Chain korrekt, .finally() Reset nach .then() Capture, val-Capture vor async verhindert stale reads.
