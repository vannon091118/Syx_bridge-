================================================================================
  SyxBridge — Tutorial
  Edition: v0.23.0  |  Stand: 2026-06-24
  Sprache: English
  Zweck: Peer-Review-Snapshot, enthaelt Dev-Tools + Doku.
        Ausgeschlossen sind nur node_modules + Secrets.
================================================================================


=== 1. Project Overview ===

SyxBridge is an AI-powered translation engine for *Songs of Syx* mods.
It automates the full workflow:

    Scan -> Extract -> Translate -> Audit -> Polish -> Export

* Language: Node.js (v18+)
* Dashboard: Web-GUI on http://localhost:3000
* Status: Alpha, solo-dev, daily use
* Plugin architecture: v0.20 Phase 1 complete (8/8 hardcodes decoupled)
* PREFLIGHT analysis: automatic DB health check before every sync
* Dual-path copy (Native Mode): Steam Workshop + AppData in parallel


=== 2. Installation & First Launch ===

System requirements:
  - Windows 10/11 (or Linux/macOS with Node 18+)
  - 8 GB RAM minimum, 16 GB recommended
  - ~500 MB free disk space (without node_modules)

Steps:
  1. Clone repo, e.g. to C:\SyxBridge\
  2. Make sure Node.js v18+ is installed:
        node --version
  3. From project root, run:
        start.bat
     The launcher runs 'npm install' if needed.
  4. Configure .env (API keys) — on first launch a .env.example
     template is copied; fill in your provider keys.
  5. Dashboard opens automatically on http://localhost:3000.


=== 3. Quickstart: CLI Mode ===

For interactive control:

    node core/index.js

Common flags:
  --auto                 non-interactive run-through
  --mod <name>           only this mod (e.g. --mod V71)
  --langs de,en          target languages (comma-separated)
  --provider groq,nvidia provider order
  --polish               force a deep-polish run
  --dry                  read-only, only emit a report
  --no-gui               do not start the dashboard

Example:

    npm run reconstruct -- --auto --mod V71 --langs de


=== 4. Quickstart: GUI Mode ===

start.bat without arguments launches the WEB dashboard on port 3000.

GUI provides:
  - Live run controls (Start/Pause/Resume/Cancel)
  - Provider status + live ETA box
  - Heartbeat timer (every 2s) and sub-phase tracking
  - DB search across all entries
  - Revision restore (per entry)
  - Flag inspect + manual override
  - Settings (provider keys, routing, cost limits)


=== 5. The Pipeline in Detail ===

Phase 1: Scan
  - Reads Workshop + AppData directories
  - Classifies files via adapter (SongsOfSyxAdapter)
  - Risk score per mod file (static + dynamic)

Phase 2: Extract
  - Decomposes strings via plugin (SongsOfSyxPlugin)
  - Shield token format: [[N]] -> __SHLD_N__ (protection)
  - Marker validation (tags, placeholders, variables)

Phase 3: Translate
  - 9 providers available (see below)
  - Risk-based routing: low risk -> fast provider
  - Cost classes: Argos=10 (fallback), Groq=4, NVIDIA=4, Ollama=1
  - Error handler: 429 -> escalating cooldown (no longer permanent disable)

Phase 4: Audit
  - Quality score per entry (translationCriticalCheck)
  - 3-tier Accept/Reject (translationLooksSafe + assessWarnings)
  - BUG-FS-003 double-shielding for non-LLM providers

Phase 5: Polish
  - Multi-provider A/B polishing (polish-arbiter)
  - Deep polish queue for subpar entries
  - Streaming writes with subPhase='writing'

Phase 6: Export
  - File export with syntax validation (exporter.js)
  - Dual-path copy (Workshop + AppData, Native Mode)
  - Marker-level validation blocks defective writes (SHIELD_RESTORE_FAIL)


=== 6. Providers (9 total) ===

| Provider          | Type           | Role                                   |
|-------------------|----------------|----------------------------------------|
| Groq              | Cloud (Llama)  | Primary provider (fast, good DE)       |
| OpenRouter        | Cloud Multi    | Polish/Audit, Free-Tier fallback       |
| Gemini            | Cloud Google   | Translation, multimodal                |
| NVIDIA NIM        | Cloud NVIDIA   | Unlimited (key only)                   |
| FCM               | Local Proxy    | Free router daemon                     |
| Ollama            | Local          | Fallback/Offline (Opt-in)              |
| Player2           | Local Desktop  | Optional (Opt-in)                      |
| Argos Translate   | Local Offline  | UI strings, low risk                   |
| Google Free       | Cloud Free     | UI strings, no key needed              |


=== 7. Database & Backup ===

SyxBridge uses SQLite in WAL mode.

File: core/translations.db
Tables:
  - translations (source, target_lang, translation, audit_stage, ...)
  - glossary (static terms from game + plugin)
  - cache_provider (provider health cache)

Backup strategy (AGENTS.md § DB-Retention):
  Format: translations_YYYY-MM-DD_{tag}.tar.gz
  Tags:    before_<fix>  /  after_<fix>  /  session_<topic>  /  release
  Path:    core/archive/dbold/


=== 8. Dev Workflow ===

Important scripts:

| Script                                  | Purpose                                |
|-----------------------------------------|----------------------------------------|
| node core/scripts/check_syntax.js       | Syntax smoke for every module          |
| node core/scripts/db_audit.js           | DB audit + health check                |
| node core/scripts/db_repair.js          | Auto-repair (<5% threshold)            |
| node core/scripts/test_providers.js     | Provider key health check              |
| node core/scripts/reset_now.js          | Hard reset (DB+cache, careful!)        |

Tests:

    node core/scripts/check_syntax.js
    node core/scripts/test_providers.js

or everything:

    npm run testline    (= lint + syntax + test)


=== 9. Plugin Architecture ===

Plugin path:  core/src/plugins/SongsOfSyxPlugin.js
Adapter path: core/src/adapters/GameAdapter.js

In the plugin:
  serializeTranslation, getFileHeader, getPromptContext, getPathRules,
  getLoreTerms, getGameTerms, classifyFile, scanMod, parseMetadata, ...

New game? Extend GamePlugin → register in plugin-registry.js → test via
plugin-boundary-contract.js (76+ dynamic interface checks).


=== 10. Roadmap ===

See PLAN.md (Root) for current roadmap.
See CHANGELOG.md for completed features.
See FREEZE_INDEX_2.md for archived decisions.


=== 11. Known Issues ===

See KNOWN_BUGS_REPORT.md for active bugs.
See FREEZE_INDEX_2.md §16 for historical bug archive.


================================================================================
  END — SyxBridge Tutorial v0.23.0
  Single Source of Truth: AGENTS.md + README.md + PLAN.md
================================================================================
